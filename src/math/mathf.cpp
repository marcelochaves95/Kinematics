#include "mathf.h"
#include <math.h>
using namespace std;

class mathf {
public:
    const float PI = M_PI;
    const float epsilon = 0.000001f;
    const float degToRad = PI * 2f / 360f;
    const float radToDeg = 1f / DegToRad;


};